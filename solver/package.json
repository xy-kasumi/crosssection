{
  "name": "crosssection-solver",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pyodide+sectionproperties FEM compute kernel for cross-section properties",
  "scripts": {
    "test": "node --import tsx tests/run-battery.ts",
    "test:unit": "node --import tsx --test 'tests/unit/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pyodide": "^0.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
