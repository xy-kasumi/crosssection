{
  "name": "crosssection-geom",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure, immutable geometry kernel for the editor (no DOM, no Pyodide)",
  "scripts": {
    "test": "node --import tsx --test 'tests/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "polygon-clipping": "^0.15.7"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
